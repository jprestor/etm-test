import React from 'react';
import './spinner.scss';

const Spinner = () => {
  return (
    <div className="container">
      <p className="spinner">loading...</p>
    </div>
  );
};

export { Spinner };
